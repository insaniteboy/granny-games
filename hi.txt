about blank:

<script>
  document.body.addEventListener('click', function () {
    // If we're inside an iframe, we're already in the "about:blank" wrapper tab — do nothing
    if (window.self !== window.top) return;

    if (location.href !== 'about:blank') {
      const newTab = window.open('about:blank', '_blank');

      if (newTab) {
        newTab.document.write(`
          <html>
            <head><title></title></head>
            <body style="margin:0;padding:0;">
              <iframe src="${location.href}" style="border:none;width:100vw;height:100vh;"></iframe>
            </body>
          </html>
        `);
        newTab.document.close();
      }

      location.href = 'about:blank';
    }
  });
</script>




<style>
  #loading-screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: #000;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 9999;
    opacity: 1;
    transition: opacity 1s ease;
  }

  #loading-screen span {
    color: #fff;
    font-family: Arial, sans-serif;
    font-size: 2rem;
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 1s ease, transform 1s ease;
  }

  #loading-screen.show-text span {
    opacity: 1;
    transform: translateY(0);
  }

  #loading-screen.fade-out {
    opacity: 0;
  }
</style>

<div id="loading-screen">
  <span>Made by ShadowedSins</span>
</div>

<script>
  const screen = document.getElementById('loading-screen');

  // Step 1: fade text up into view
  setTimeout(() => {
    screen.classList.add('show-text');
  }, 100);

  // Step 2: fade everything out (text + black background) around the 3s mark
  setTimeout(() => {
    screen.classList.add('fade-out');
  }, 2000);

  // Step 3: remove from DOM after fade-out transition finishes
  setTimeout(() => {
    screen.remove();
  }, 3000);
</script>
